---
title: Platform
desc: Android Repository Viewer
---
<div class="sixteen columns">
  <h1 class="page-title">{{ title }}<span class="line"></span></h1>
  {{#each android.sdk-repository.platform}}
  <h2>{{ description }}</h2>
  <dl class="dl-horizontal">
    <dt>version</dt>
    <dd>{{ version }}</dd>
    <dt>api-level</dt>
    <dd>{{ api-level }}</dd>
    <dt>revision</dt>
    <dd>{{ revision }}</dd>
    <dt>size</dt>
    <dd>{{ size }}</dd>
    <dt>checksum</dt>
    <dd>{{ checksum }}</dd>
    <dt>url</dt>
    <dd>{{ url }}</dd>
  </dl>
  {{/each}}
</div>
